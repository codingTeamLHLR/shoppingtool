    <section id="searchAndFilter">

        <form action="/products" method="GET">

            <input id="search" type="text" name="word" placeholder="What are you looking for?">
            <input id="maxPrice" type="text" name="maxPrice" placeholder="maximum price">
            <label> 
                List:
                <select name="list" multiple>

                {{#each result}}
                    <option value="{{this.list._id}}">{{this.list.name}}</option>
                {{/each}}
                    
                </select>
            </label>
            {{!-- 
            {{#each lists}}
                <label><input type="checkbox" name="{{this.name}}" value="{{this._id}}"><span>{{this.name}}</span></label>
            {{/each}} --}}

            <button  type="submit" > Find </button>
        </form>
    </section>



<section id="allProducts">

{{#each result}}
<div class="product">

    {{#if image}}
    <img src="{{this.image}}"/>
    {{else}}
    <img src="https://t4.ftcdn.net/jpg/04/70/29/97/360_F_470299797_UD0eoVMMSUbHCcNJCdv2t8B2g1GVqYgs.jpg"/>
    {{/if}}



    <div class="productHeader">
        <p class="productName">{{this.name}}</p>
        <p class="productPrice">{{this.price}} €</p>
        <button id="linkButton"><a href="{{this.link}}" target="_blank">Product Page</a></button>
    </div>

    {{#if notes}}
    <p class="notes">Notes:<br>{{this.notes}}</p>
    {{/if}}

    <div class="editButtons">
        <button id="editButton"><a href="/products/{{this._id}}/edit">Edit</a></button>
        <form id="/products/{{this._id}}/delete" method="POST">
            <button class="editButton">Delete</button>
        </form>
    </div>

</div>
{{/each}}

<div class="product">
     <a href="/products/create">Create new Product</a>
</div>

</section>