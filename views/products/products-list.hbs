    <section id="searchAndFilter">

        <form action="/products" method="GET">

            <input id="search" type="text" name="word" placeholder="What are you looking for?"  value="{{data.filter.name.$regex}}">
            <input id="maxPrice" type="text" name="maxPrice" placeholder="Max. price" value="{{data.filter.price.$lte}}">

            <button id="findButton" type="submit" > Find </button>

            <br>

            <input class="listOption" type="radio" id="html" name="list" value="null" onChange="this.form.submit()" >
            <label>Show All</label>

            {{#each data.lists}}
                {{#if (eq this._id ../data.filter.list) }}
                    <input class="listOption" type="radio" id="html" name="list" value="{{this._id}}" onChange="this.form.submit()" checked >
                {{else}}
                    <input class="listOption" type="radio" id="html" name="list" value="{{this._id}}" onChange="this.form.submit()"  >
                {{/if}}
                <label>{{this.name}}</label>
            {{/each}}
        </form>
    </section>



{{#if data.showCreateModal}}
    <div id="overlay">
        {{> productcreate}}
    </div>
{{/if}}

{{#if data.showEditModal}}
    <div id="overlay">
        {{> productedit}}
    </div>
{{/if}}



<section id="allProducts">

<div class="product" id="addProductCard">
     <a href="/products/create">Add Product</a>
</div>

{{#each data.products}}
<div class="product">

    {{#if image}}
    <img src="{{this.image}}"/>
    {{else}}
    <img src="https://t4.ftcdn.net/jpg/04/70/29/97/360_F_470299797_UD0eoVMMSUbHCcNJCdv2t8B2g1GVqYgs.jpg"/>
    {{/if}}



    <div class="productHeader">
        <p class="productName">{{this.name}}</p>
        <p class="productPrice">{{this.price}} €</p>
        <button id="linkButton"><a href="{{this.link}}" target="_blank">Product Page</a></button>
    </div>

    {{#if notes}}
    <p class="notes">Notes:<br>{{this.notes}}</p>
    {{/if}}

    <div class="editButtons">
        <button id="editButton"><a href="/products/{{this._id}}/edit">Edit</a></button>
        <form action="/products/{{this._id}}/delete" method="POST">
            <button class="editButton">Delete</button>
        </form>
    </div>

</div>
{{/each}}

</section>